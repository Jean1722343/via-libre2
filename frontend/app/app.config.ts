// Paleta editorial cálida: terracota (marca), verde pino (confirmar),
// ámbar (programado), neutros cálidos (stone).
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'terracota',
      secondary: 'pino',
      success: 'pino',
      warning: 'ambar',
      neutral: 'stone',
    },
  },
})
